#include "main.h"
#include <stdio.h>
void _puts(char *str)
/**
 * _puts - prints a string to stdout
 * @str: pointer to the string to print
 */
int main(void)
{

char *str;
str = "Holberton!";
_puts(str);
return (0);

}
