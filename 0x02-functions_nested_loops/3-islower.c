#include "main.h"
/**
 * _islower - returns 1 if c is lowercase
 *
 * @c: is lowercase
 * Return: returns 0 if c is not lowercase
 */
int _islower(int c)
{
int c;

if (c >= 97; c <= 122)
{
return (1);
}
{
return (0);
}
}
